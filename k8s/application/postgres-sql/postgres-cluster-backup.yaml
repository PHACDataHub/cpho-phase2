# this file is not synced with flux
apiVersion: postgresql.cnpg.io/v1
kind: Backup
metadata:
  name: postgres-cluster-backup
  namespace: server
spec:
  cluster:
    name: cpho-postgres-cluster
---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: postgres-cluster-backup
  namespace: server
spec:
  schedule: "0 0 0 * * *"
  backupOwnerReference: self
  cluster:
    name: cpho-postgres-cluster
